image:
  - Visual Studio 2017
  - Ubuntu
clone_folder: C:\Projects\TestContinuousIntegration
build_script:
  - cmake --version
  - mkdir build
  - cd build
  - cmake ..
  - cd ..
  - cmake --build build --config Debug
artifacts:
  - path: 'build\bin\*.*'